<%= render 'welcome/nav' %>

<h1>Users</h1>

<div class="row">
<% @users.each do |user| %>
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
    <%= link_to user  do%>
     <%= image_tag user.avatar.url(:small)%>
     <%end%>
      <div class="caption">
        <h3><%=user.first_name%> <%= user.last_name %></h3>
        <p><%= user.email %></p>
       <p><%= link_to "Add Friend", friendships_path(:friend_id => user), :method => :post %></p>
        <p><div class="btn btn-primary" role="button"><%= link_to 'View Profile', user %></a></p>
      </div>
    </div>
  </div>
</div>
<%end%>


